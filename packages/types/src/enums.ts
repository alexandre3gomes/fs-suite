export enum FlightType {
  VFR = 'VFR',
  IFR = 'IFR',
}

export enum PlanStatus {
  DRAFT = 'DRAFT',
  SAVED = 'SAVED',
  ARCHIVED = 'ARCHIVED',
}

export enum OAuthProvider {
  GOOGLE = 'google',
}
