# OS
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Build outputs
.next/
dist/
out/
build/

# Turbo
.turbo/

# Test coverage
coverage/
playwright-report/
test-results/

# Environment files
.env
.env.*
!.env.example
!.env.example.production

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Prisma (migrations are tracked in git)
apps/api/prisma/data/.enrich-cache/
apps/api/prisma/*-cache.csv

# Expo
.expo/
apps/app/dist/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Misc
*.tsbuildinfo

# Database dumps — never commit (contain prod data)
*.sql
*.dump
# …but Prisma migration SQL MUST be versioned, or `prisma migrate deploy`
# has nothing to apply on prod. The broad *.sql rule above was silently
# excluding new migrations (older ones predate the rule).
!apps/api/prisma/migrations/**/*.sql
