# Expo requires hoisted module resolution so Metro can traverse the dependency tree.
# Without this, pnpm's strict symlink strategy prevents Metro from resolving
# native packages (e.g. expo-router/entry) at build time.
node-linker=hoisted
